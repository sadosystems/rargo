[package]
name = "abrasive"
version = "0.0.1"
edition = "2024"
description = "A simple remote cargo CLI"
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/sadosystems/abrasive"

[dependencies]
crep.workspace = true
serde.workspace = true
toml.workspace = true